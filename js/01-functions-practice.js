/*
//STEP 1
function halfNumber(num){
    let halfnum = num / 2;
    console.log(`half of ${num} is ${halfnum}`);
    return halfnum;
}
halfNumber(125);
//STEP 2
function squareNumber(num){
    let squarenum = num * num;
    console.log(`${num} squared is ${squarenum}`);
    return squarenum;
}
squareNumber(7);
//STEP 3
function percentOf(num1, num2){
    let percentNum = Math.round((num1 / num2) * 100)
    console.log(`${num1} is ${percentNum}% of ${num2}`);
    return percentNum;
}
percentOf(50, 150);
//STEP 4
function findModulus(num1, num2){
    let modulo = (num1 % num2);
    console.log(`${modulo} is the modulus of ${num1} and ${num2}`);
    return modulo;
}
findModulus(15, 5);
*/